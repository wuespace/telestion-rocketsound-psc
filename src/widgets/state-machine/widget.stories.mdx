import {
	Meta,
	Story,
	Canvas,
	Description,
	ArgsTable
} from '@storybook/addon-docs/blocks';

import { StateMachine } from './widget';
import { Badge } from './badge';

<Meta title="Widgets/State Machine Widget" component={StateMachine} />

# State Machine Widget

```tsx
import { StateMachine } from '/src/widgets/state-machine/widget';
```

<Description of={StateMachine} />

## Examples

export const states = [
	{
		currentState: 'Preflight',
		nextState: 'Launch',
		stateClass: 'Preflight'
	},
	{
		currentState: 'Launch',
		nextState: 'In-Flight',
		stateClass: 'Preflight'
	},
	{
		currentState: 'Flying',
		nextState: 'Landing',
		stateClass: 'Flight'
	},
	{
		currentState: 'Landing',
		nextState: 'Landed',
		stateClass: 'Flight'
	},
	{
		currentState: 'Landed',
		nextState: 'n/a',
		stateClass: 'Post-Flight'
	}
];

<Canvas>
	<Story name="Preflight" args={states[0]}>
		{args => <StateMachine {...args} />}
	</Story>
	<Story name="Launch" args={states[1]}>
		{args => <StateMachine {...args} />}
	</Story>
	<Story name="Flying" args={states[2]}>
		{args => <StateMachine {...args} />}
	</Story>
	<Story name="Landing" args={states[3]}>
		{args => <StateMachine {...args} />}
	</Story>
	<Story name="Landed" args={states[4]}>
		{args => <StateMachine {...args} />}
	</Story>
</Canvas>

## Properties

<ArgsTable of={StateMachine} />
